'use strict';

var android = require('../android.js');
var preserve = require('promise-preserve');

android.startOrCreate()
  .then(function(device) {
    console.log(device.id);
    return device.id;
  })
  .then(preserve(android, 'ensureReady'))
  .then(preserve(android, 'stop'));
